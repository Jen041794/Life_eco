import { Alert } from 'react-bootstrap'

// 骨架：購物車（之後用 localStorage 存購物車項目、可改數量、結帳）
export default function CartPage() {
  return (
    <div>
      <h3 className="mb-3">購物車</h3>
      <Alert variant="secondary">🚧 建構中：購物車清單與「前往結帳」之後補上。</Alert>
    </div>
  )
}
